apiVersion: v1
kind: ResourceQuota
metadata:
  name: manual-managed-quota
  namespace: manual-managed
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
    pods: "10"